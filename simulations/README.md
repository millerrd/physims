# Simulations

This directory contains self-contained JavaScript simulation files.

Each simulation is a single `.js` file that is loaded by its corresponding
simulation HTML page. See the repository README for instructions on adding
a new simulation.
